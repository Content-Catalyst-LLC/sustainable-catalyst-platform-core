# Install and test v2.67.0

Use `deploy_and_validate_platform_core_v2_67_0_macos.sh` against the complete release bundle. The validator verifies checksums, frozen source manifest, syntax, the release-critical regression list, migration `0071`, and Visual Predictive Intelligence invariants before Git promotion.
