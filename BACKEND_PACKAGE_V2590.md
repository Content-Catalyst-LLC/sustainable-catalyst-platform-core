# Platform Core v2.59.0 Standalone Backend Package

The standalone backend includes the FastAPI service, additive migration `0063`, predictive-decision models/services/routes/tests, public SDK sources, and validation tooling. It is intended for backend-first Contabo deployment after the frozen GitHub tag is promoted.
