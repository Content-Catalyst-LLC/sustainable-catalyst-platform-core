# Platform Core v2.77.0 Install and Test

Run `deploy_and_validate_platform_core_v2_77_0_macos.sh` against the exact release bundle. The validator verifies bundle hashes, the frozen manifest, syntax, inherited release tests, migration `0081`, research-intelligence invariants, and Git promotion before production deployment.
