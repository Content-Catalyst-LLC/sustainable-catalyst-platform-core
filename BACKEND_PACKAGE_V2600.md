# Backend Package v2.60.0

The standalone backend archive contains the FastAPI backend, migration `0064`, predictive-package APIs/services/models, validation script, public SDK sources, and release documentation required to deploy the v2.60.0 backend independently of WordPress.
