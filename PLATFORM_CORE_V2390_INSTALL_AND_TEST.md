# Platform Core v2.39.0 — Install and Test

1. Run bundle-only validation.
2. Run release-critical validation and GitHub promotion.
3. Copy `DEPLOY_PLATFORM_CORE_V2390_CONTABO.sh` to the VPS and run it.
4. Verify public Core health reports `2.39.0` and `/v1/research-visual-explanations/readiness` reports `migration_0043_applied=true`.
5. Install the v2.39.0 WordPress plugin.
6. Optional status shortcode: `[sc_platform_core_research_visual_explanation_status]`.
