# Install and Test v2.21.0

Use `deploy_and_validate_platform_core_v2_21_0_macos.sh` with the v2.21.0 release bundle. The installer verifies checksums, manifest, syntax, file-by-file tests, migrations through 0024, inherited validators, the new resilience validator, and only then invokes the Git push script.
