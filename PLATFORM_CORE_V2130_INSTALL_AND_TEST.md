# Platform Core v2.13.0 — Install and Test

Use `deploy_and_validate_platform_core_v2_13_0_macos.sh` with the v2.13.0 release bundle. The installer verifies component checksums, the immutable repository manifest, syntax, deterministic tests, migrations through `0016`, scientific-service-fabric readiness and the inherited Core validation line before promotion.
