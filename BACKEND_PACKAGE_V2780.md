# Platform Core v2.78.0 Backend Package

The backend package contains the FastAPI Core runtime, migration ledger through 0082, Hypothesis & Competing Explanation Engine router/service/models/tests, public SDK sources, and migration/validation scripts. Deploy through the frozen v2.78.0 repository/tag using `DEPLOY_PLATFORM_CORE_V2780_CONTABO.sh`.
