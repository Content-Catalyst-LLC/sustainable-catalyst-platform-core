# Platform Core v2.15.0 — Install and Test

Use the release bundle installer. It verifies checksums, the immutable manifest, Python/PHP/JavaScript/Bash syntax, runs every backend test file separately, migrates a clean database through 0018, validates inherited reliability/facility/humanitarian/country/scientific/exchange planes, validates the v2.15.0 scale plane, and only then enters Git promotion.
