# Platform Core v2.12.0 — Install and Test

Use `deploy_and_validate_platform_core_v2_12_0_macos.sh` with the v2.12.0 release bundle. The installer verifies component SHA-256 checksums, the immutable repository manifest, syntax, deterministic tests, migrations through `0015`, and country-evidence readiness before promotion.
