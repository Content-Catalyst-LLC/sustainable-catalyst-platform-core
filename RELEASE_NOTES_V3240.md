# Platform Core v3.24.0 — Execution Environment & Dependency Provenance

Added:
- `sc.core.execution-environment-provenance.v1`
- governed environment provenance records;
- dependency nodes and dependency-graph edges;
- lockfile/project/manifest hash records;
- stable environment provenance fingerprints;
- exact environment requirement verification;
- dependency version/hash drift detection;
- lockfile drift detection;
- environment comparison reports;
- Julia v0.3.0 registered reference runtime;
- public environment-provenance contract endpoint;
- ten focused release tests.

No database migration is introduced.

Next: Platform Core v3.25.0 — Unified Computational Job Runtime.
