# Install and test v2.40.0

Run the bundle-only gate, then the full Mac validation/promotion script. Deploy the backend on Contabo before installing the WordPress plugin. Production must have migration `0043` and the complete v2.39 Research Librarian Visual Explanation schema before `0044` is applied.
