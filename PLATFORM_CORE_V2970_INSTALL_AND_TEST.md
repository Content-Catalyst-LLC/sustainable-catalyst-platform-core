# Install and Test v2.97.0

Run `python3 -S scripts/validate_v2970_release.py`, then with backend dependencies available run `PYTHONPATH=backend python backend/scripts/validate_platform_research_integration_certification.py` and the v2.97 tests. Production deployment uses `DEPLOY_PLATFORM_CORE_V2970_CONTABO.sh`.
