# v2.21.0 Multi-Region Resilience Audit

Release invariants: automatic failover disabled; infrastructure actuation by Core disabled; write failover requires replication safety; read-only degraded mode is explicit; public status is aggregate-only; certification gating is opt-in; evidence semantics are unchanged.
