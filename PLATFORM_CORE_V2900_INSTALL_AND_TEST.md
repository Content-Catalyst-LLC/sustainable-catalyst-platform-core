# Install and Test v2.90.0

1. Validate the bundle with `deploy_and_validate_platform_core_v2_90_0_macos.sh`.
2. Promote with `PUSH_PLATFORM_CORE_V2900_FINAL.sh`.
3. Deploy with `DEPLOY_PLATFORM_CORE_V2900_CONTABO.sh`.
4. Verify `/health` and `/v1/research/workflows/readiness`.
