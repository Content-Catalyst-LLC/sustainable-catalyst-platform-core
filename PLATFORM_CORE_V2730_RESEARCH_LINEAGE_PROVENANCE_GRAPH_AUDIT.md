# v2.73.0 Audit — Research Lineage & Provenance Graph

Contract: `sc.research.lineage-graph.v1`. Migration `0077` is additive. Eight registries cover graphs, nodes, explicit edges, activities, transformations, source bindings, deterministic declared traces, and immutable snapshots. Core traces only declared provenance.
