# v2.36.1 Audit

Migration 0040; deterministic sampling; Sobol/Morris post-processing; ensemble statistics; empirical probability estimation; runtime handoff manifests; persisted compute provenance; no arbitrary code execution; no automatic truth promotion.
