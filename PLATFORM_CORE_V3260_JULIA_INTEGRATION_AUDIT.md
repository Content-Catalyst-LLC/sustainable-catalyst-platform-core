# Platform Core v3.26.0 — Julia Runtime Core Integration

Contract: `sc.core.julia-runtime-integration.v1`

Adds strict Julia v0.3.0 registration, provider handshake validation, Core job → Julia request mapping, Julia response normalization, and environment provenance normalization. Core remains non-executing. No migration.

Next: Platform Core v3.27.0 — AI Model & Model-Version Object Model.
