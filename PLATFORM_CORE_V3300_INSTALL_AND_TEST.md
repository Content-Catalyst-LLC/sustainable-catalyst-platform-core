# Platform Core v3.3.0 Install & Test

Run `python3 -S scripts/validate_v3300_release.py`, backend validation, deterministic release tests, then deploy migration 0105 and verify `/v1/analytics/statistical-reasoning/readiness`.
