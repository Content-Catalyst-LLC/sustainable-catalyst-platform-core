# v2.31.0 Install and Test

Run the Mac release validator, push/tag v2.31.0, deploy to Contabo with migration 0034, then install the WordPress plugin and verify `[sc_platform_core_system_maps_status]`.
