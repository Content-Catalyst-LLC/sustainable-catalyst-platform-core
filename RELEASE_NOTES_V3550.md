# Platform Core v3.55.0 — Prolog Logic & Constraint Runtime

Adds a first-class governed SWI-Prolog runtime for logic, constraint, relation, contradiction, and temporal reasoning. No arbitrary Prolog source is accepted. Workspace and Research Lab may explicitly bind the runtime through the Unified Runtime API. No database migration.
