# Platform Core v2.80.0 Install & Test

Run `deploy_and_validate_platform_core_v2_80_0_macos.sh` against the release bundle for local release validation. Promote with `PUSH_PLATFORM_CORE_V2800_FINAL.sh`, then deploy on Contabo with `DEPLOY_PLATFORM_CORE_V2800_CONTABO.sh`. Verify `/health` and `/v1/research/conclusions/readiness`.
