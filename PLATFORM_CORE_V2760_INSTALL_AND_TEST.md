# Platform Core v2.76.0 Install and Test

Run `deploy_and_validate_platform_core_v2_76_0_macos.sh` against the exact release bundle. The validator verifies bundle hashes, the frozen manifest, syntax, the inherited release test list, migration `0080`, notebook invariants, and Git promotion before production deployment.
