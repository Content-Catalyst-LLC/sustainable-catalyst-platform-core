# Install and test v2.86.0
Run `python3 -S scripts/validate_v2860_release.py`, then execute `backend/scripts/validate_research_protocols.py` with backend dependencies installed and `PYTHONPATH=backend`.
