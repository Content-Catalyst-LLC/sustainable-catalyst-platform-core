# Platform Core v2.14.0 — Cross-Product Evidence Exchange
SC_CORE_CROSS_PRODUCT_EXCHANGE_ENABLED=true
# Exchange package contents remain internal authenticated records.
# v2.14.0 does not automatically push packages to downstream product services.
