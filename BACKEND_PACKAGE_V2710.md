# Platform Core v2.71.0 Standalone Backend

The standalone backend package contains the FastAPI application, migration `0075`, Cross-Product Visual Runtime Integration router/service, schemas, tests, validators, public SDK source, and runtime/deployment support required by Platform Core. It does not include the WordPress plugin or complete repository documentation history.
