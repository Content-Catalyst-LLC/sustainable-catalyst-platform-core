# Platform Core v3.1.0 — Analytical Runtime Provider Contract
SC_CORE_ANALYTICAL_RUNTIME_PROVIDER_CONTRACT_ENABLED=true
# Catalyst Analytics R is registered by migration 0103 as runtime=r, execution_host=workspace.
# No R transport URL or credential is stored by this build; Workspace owns execution/authentication.
