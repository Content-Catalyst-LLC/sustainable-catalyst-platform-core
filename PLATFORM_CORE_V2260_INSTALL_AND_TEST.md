# Platform Core v2.26.0 Install & Test

Run the packaged macOS deploy/validate script from Downloads. The promotion gate validates migration 0029, the v2.26.0 contract, manifest integrity, secret scanning, syntax, and backend test modules before push.
