# Backend Package — Platform Core v2.96.0

The backend adds the Unified Research Runtime Contract at `/v1/research/runtime-contract` and the public read boundary at `/api/v1/research/runtime-contract`. Migration `0100` is additive and requires v2.95.0 / migration `0099` in production.
