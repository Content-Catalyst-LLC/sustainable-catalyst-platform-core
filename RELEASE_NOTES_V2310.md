# Platform Core v2.31.0 — System Maps

- Migration `0034`.
- Adds governed System Map records bound to v2.29 `system-map` visual reasoning objects.
- Adds explicit boundaries, domains, element memberships, and saved views.
- Adds structural validation without causal inference.
- Adds deterministic compilation to v2.30 immutable visualization specifications.
- Adds public read metadata and Python/JavaScript SDK helpers.
- Adds WordPress shortcode `[sc_platform_core_system_maps_status]`.
- Preserves the non-rendering Core boundary.
